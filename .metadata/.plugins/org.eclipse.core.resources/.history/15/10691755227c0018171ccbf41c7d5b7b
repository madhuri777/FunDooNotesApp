package com.bridgeit.fundoonotes.utility;

import org.springframework.stereotype.Component;

@Component
public class SenderRabbitMQ {

	@Autowired
	RabbitTemplate rabbitTemplate;
	
	public SenderRabbitMQ(RabbitTemplate rabbitTemplate) {
		this.rabbitTemplate=rabbitTemplate;
		System.out.println("Sender");
	}
	
	public void sendMessage() {
		rabbitTemplate.convertAndSend(RabbitConfiguration.topicExchangeName,"lazy.orange.rabbit", "My first Mail");
		//log.info("The message has been sent to the queue.");
		}
}
